name: Snyk Security Scan (Debug)

on:
  push:
    branches: [ "master" ]
  pull_request:

jobs:
  snyk-scan:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install all dependencies
        run: npm install --legacy-peer-deps

      - name: DEBUG - Show current directory and list files
        run: |
          echo "--- Current Directory ---"
          pwd
          echo "--- Files in Directory ---"
          ls -la
          echo "--- Checking for package.json ---"
          cat package.json | grep "build" || echo "Build script not found in package.json"

      - name: DEBUG - List available npm scripts
        run: npm run

      - name: Build the application
        run: npm run build

      - name: Run Snyk to check for vulnerabilities
        uses: snyk/actions/node@master

        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
          args: --sarif-file-output=snyk.sarif@v3

      - name: Upload result to GitHub Code Scanning
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: snyk.sarif
